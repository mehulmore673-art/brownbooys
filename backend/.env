# ── Database ──────────────────────────────────────────────
# MongoDB Atlas connection string. Create a DEDICATED database user
# for this app (not your Atlas account password) and restrict its
# network access to your backend host's IP / 0.0.0.0 only if required.
MONGO_URI=mongodb://brownbooys:Brown1988@ac-phi1sap-shard-00-00.k86ou0d.mongodb.net:27017,ac-phi1sap-shard-00-01.k86ou0d.mongodb.net:27017,ac-phi1sap-shard-00-02.k86ou0d.mongodb.net:27017/?ssl=true&replicaSet=atlas-qm2z7v-shard-0&authSource=admin&appName=Cluster0

# ── Server ────────────────────────────────────────────────
PORT=5000

# Comma-separated list of allowed frontend origins (no trailing slash).
# Add your Netlify/Vercel URLs for both the customer site and admin panel,
# plus http://localhost:3000 for local development.
FRONTEND_ORIGINS=https://brownbooysadmin.netlify.app/,https://brownbooys.netlify.app/,http://localhost:3000

# ── Razorpay ──────────────────────────────────────────────
# Use LIVE keys only in production. Never commit real values.
RAZORPAY_KEY_ID=rzp_live_SuROxOuA2k1qMf
RAZORPAY_KEY_SECRET=7WMWGwuHZcRUnRcszZ7Xnsx4

# ── Cloudinary ────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=BrownBooys
CLOUDINARY_API_KEY=159472426888495
CLOUDINARY_API_SECRET=Xb6GN4-xIFhCThvGiy7VdizdrdU

# ── Admin ─────────────────────────────────────────────────
# Long, random, unique string (32+ chars). Rotate immediately if it
# was ever committed to git or shared anywhere.
ADMIN_PASSWORD=brownbooysadmin

# Separate secret used to sign admin session tokens (JWTs). Use a
# different long random string than ADMIN_PASSWORD — generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=brownbooys@1988

